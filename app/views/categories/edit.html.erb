<h1 class="text-center font-italic font-weight-light mb-5">Edit <%= @category.name %></h1>

<%= render 'form' %>

<div class="text-center">
	<h5><%= link_to 'Cancel editing', category_path(@category), class: "btn btn-warning", data: {confirm: "Are you sure you want cancel creation?"} %>
	</h5>
</div>