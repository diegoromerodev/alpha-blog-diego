<div class="text-center">
	<h1>All Articles</h1>
	<h5><%= link_to 'Create new article', new_article_path, class: "btn btn-info" %></h5>

<div class="flickr_pagination mt-4">
	<%= will_paginate @articles, :container => false %>
</div>

<%= render 'articles' %>

<div class="container mb-5">
	<div class="row justify-content-md-center mt-4">
		<div class="col-8">
			<h6 class="text-left"><%= link_to 'Go to the home page instead', root_path, class: "btn btn-info" %></h6>
		</div>
	</div>
</div>

<div class="flickr_pagination mb-4">
	<%= will_paginate @articles, :container => false %>
</div>